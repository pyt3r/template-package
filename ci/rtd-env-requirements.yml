name: rtd-env
channels:
  - conda-forge
  - defaults
dependencies:
  # build dependencies:
  - pip
  - pyyaml
  - sphinx=2.1.4
  - sphinx_rtd_theme
  - sphinx-gallery
  - pip:
      - rst2pdf=0.98
  # ./examples dependencies
  - pandas
  - matplotlib