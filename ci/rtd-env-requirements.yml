name: rtd-env
channels:
  - conda-forge
  - defaults
dependencies:
  # build dependencies:
  - pip
  - pyyaml
  - sphinx
  - sphinx_rtd_theme
  - sphinx-gallery
  - pip:
      - rst2pdf
  # ./examples dependencies
  - pandas
  - matplotlib